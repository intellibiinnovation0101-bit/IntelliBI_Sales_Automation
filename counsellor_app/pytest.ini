[pytest]
# The validation suite lives in validation/ as verify_*.py (purpose-named files);
# this tells pytest where to look and which files to collect. Run from this folder:
#     python -m pytest -q
testpaths = validation
python_files = verify_*.py

# IntelliBI Counsellor App — runtime dependencies
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.6
PyYAML>=6.0
tzdata>=2024.1 ; platform_system == "Windows"   # Windows ships no IANA tz DB
bcrypt>=4.1
gspread>=6.0
google-auth>=2.30
# optional, only if Google OAuth login is enabled:
#   google-auth-oauthlib>=1.2
